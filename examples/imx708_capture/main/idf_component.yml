dependencies:
  idf: ">=5.3"
  espressif/esp_video: "*"
  # The IMX708 add-on driver lives in this repo's components/ directory and is
  # picked up via EXTRA_COMPONENT_DIRS in the top-level project CMakeLists.
