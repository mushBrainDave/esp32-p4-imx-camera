dependencies:
  idf: ">=5.3"
  espressif/esp_video: "*"
  # esp_hosted / esp_wifi_remote come in through components/imx_wifi, which
  # declares them itself so every WiFi example gets the same pinned pair.
